# Warp2Api 环境变量配置示例（最小可用）

# 监听地址与端口
HOST=127.0.0.1
PORT=28889

# 对外 API 鉴权
API_TOKEN=change_me_to_secure_token

# 管理后台鉴权模式: off / local / token
WARP_ADMIN_AUTH_MODE=off
# 当 WARP_ADMIN_AUTH_MODE=token 时启用
# ADMIN_TOKEN=change_me_admin_token

# Warp 认证
# 推荐：通过 /admin/tokens 导入 refresh token 到数据库（SSOT）
# 可选兜底：仅在数据库为空时，才使用 WARP_REFRESH_TOKEN_B64
# WARP_REFRESH_TOKEN_B64=base64_encoded_refresh_payload

# Token 池准实时监控
WARP_POOL_MONITOR_INTERVAL_SECONDS=120
WARP_POOL_TOKEN_REFRESH_INTERVAL_SECONDS=180
WARP_POOL_MONITOR_MAX_PARALLEL=3
WARP_POOL_QUOTA_RETRY_LEAD_SECONDS=300

# 可选：数据库路径（默认 data/token_pool.db）
# WARP_TOKEN_DB_PATH=/absolute/path/to/token_pool.db
